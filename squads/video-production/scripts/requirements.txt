# ffmpeg fallback when no system ffmpeg is on PATH (static build)
imageio-ffmpeg>=0.5
# optional: word-level voiceover timestamps (falls back to a script-based estimate without it)
faster-whisper>=1.0
# optional: text on title cards
Pillow>=10
