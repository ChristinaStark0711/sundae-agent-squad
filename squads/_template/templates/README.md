Templates are the shapes of the files the squad exchanges: the brief a human fills in, and the
JSON/Markdown files each phase writes into `projects/<slug>/work/`. Every agent that writes a
file follows the template exactly so the next agent can read it without guessing.
