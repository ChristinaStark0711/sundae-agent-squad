Helper scripts the agents run. Python 3.10+, standard library only unless the squad's
`requirements.txt` says otherwise. Each script takes a project path, writes into
`projects/<slug>/work/`, prints a short summary, and exits non-zero on failure.
